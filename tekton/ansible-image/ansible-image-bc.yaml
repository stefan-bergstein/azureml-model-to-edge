kind: ImageStream
apiVersion: image.openshift.io/v1
metadata:
  name: ansible-playbook
spec:
  lookupPolicy:
    local: true
---
apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: ansible-playbook
spec:
  source:
    git:
      ref: 'dev'
      uri: 'https://github.com/stefan-bergstein/azureml-model-to-edge.git'
    contextDir: 'tekton/ansible-image'
  strategy:
    dockerStrategy:
      dockerfilePath: Containerfile
    type: Docker
  output:
    to:
      kind: ImageStreamTag
      name: ansible-playbook:latest
  triggers:
    - type: ImageChange
      imageChange: {}
    - type: ConfigChange